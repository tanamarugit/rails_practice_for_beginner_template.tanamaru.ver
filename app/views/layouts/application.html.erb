<!DOCTYPE html>
<html>
  <head>
    <title>RailsPractice</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |message_type, message| %>
      <%= message %>
    <% end %>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand">質問アプリ</a>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/questions">質問一覧</a>
        </li>
        <li>
          <% if current_user %>
            <%= link_to 'ログアウト', logout_path, method: :delete, data: { confirm: 'ログアウトしますか?' }, class: 'nav-link' %>
          <% else %>
            <%= link_to 'ログイン', sessions_new_path, class: 'nav-link' %>
          <% end %>
        </li>
        <li>
          <% if current_user %>
            <p>ログイン中のユーザー<%= current_user.name %>さん</p>
          <% end %>
        </li>
        <li>
          <% if current_user %>
            <a class="nav-link active" aria-current="page" href="/users">ユーザー一覧</a>
          <% end %>
        </li>
        <li>
          <% if current_user %>
            <a class="nav-link active" aria-current="page" href="/questions/new">質問新規作成</a>
          <% end %>
        </li>
        <li>
          <% if current_user == nil %>
            <a class="nav-link active" aria-current="page" href="/users/new">ユーザー新規作成</a>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
    <%= yield %>
  </body>
</html>
